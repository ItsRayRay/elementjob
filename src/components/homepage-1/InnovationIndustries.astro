---
import { getCollection } from 'astro:content';

const allIndustriesEntries = await getCollection('industries', ({ data }) => {
  return data.draft !== true;
});
---

<div class="mx-auto max-w-7xl sm:px-16 py-10 bg-gray-50">
  <h2 class="text-center text-3xl font-bold tracking-tight text-gray-800 sm:text-4xl">Application Area</h2>
  <p class="mx-auto mt-6 max-w-xl text-lg leading-8 text-gray-600 px-6">Incididunt sint fugiat pariatur cupidatat consectetur sit cillum anim id veniam aliqua proident excepteur commodo do ea.</p>
  <div role="list" class="mx-auto mt-16 grid max-w-2xl auto-rows-fr grid-cols-1 gap-8 sm:mt-20 lg:mx-0 lg:max-w-none lg:grid-cols-3 px-6">
    {allIndustriesEntries.map((d) => (
      <article class="group/item relative isolate flex flex-col justify-end overflow-hidden rounded-2xl bg-gray-900 px-8 pb-8 pt-80 sm:pt-48 lg:pt-80">
        <img src={d.data.thumbnail} alt="" class="absolute inset-0 -z-10 h-full w-full object-cover" />
        <div class="absolute inset-0 -z-10 bg-gradient-to-t from-gray-900 via-gray-900/40 group-hover/item:via-gray-900/50" />
        <div class="absolute inset-0 -z-10 rounded-2xl ring-1 ring-inset ring-gray-900/10" />
        <h3 class="text-lg font-semibold text-white"><a href={`/industry/${d.slug}`}>{d.data.title}</a></h3>
        <p class="mt-2 text-sm text-gray-300">{d.data.description}</p>
      </article>
    ))}
  </div>
  <div class="mt-10 flex items-center justify-center gap-x-6">
    <a href="/industry" class="rounded-md bg-primary px-3.5 py-2.5 text-sm font-semibold text-gray-100 shadow-sm hover:bg-primary/70 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">See all industries</a>
  </div>
</div>
